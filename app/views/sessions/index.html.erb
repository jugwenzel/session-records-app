<h1>Sessions</h1>

The total of recorded sessions is <%= Session.public_count %>!

<ul>
  <% @sessions.each do |session| %>
    <% unless session.archived? %>
      <li>
        <%= link_to session.title, session %>  
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "Create new Session", new_session_path %>